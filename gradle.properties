# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE are ignored when building from the command line, check
# build.gradle files instead.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for headless machines. See also http://gradle.org/docs/current/userguide/gradle_daemon.html
# org.gradle.jvmargs=-Xmx1536m

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
# org.gradle.parallel=true

# AndroidX package structure to make it clearer which packages are being used in your project.
# It can be disabled with the property org.gradle.android.cache-fix.ignoreVersionCheck=true.
android.useAndroidX=true

# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official

# Enable BuildConfig generation
android.defaults.buildfeatures.buildconfig=true

# Disable unused resources
android.enableUnusedResources=false

# Version information
APP_VERSION_CODE=1
APP_VERSION_NAME=1.0.0

# Signing configuration (for CI/CD)
RELEASE_STORE_FILE=keystore.jks
RELEASE_STORE_PASSWORD=AutoPlaneMode123!
RELEASE_KEY_ALIAS=autoplanemode
RELEASE_KEY_PASSWORD=AutoPlaneMode123!
